import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './Register and Login/app.jsx';
import './Register and Login/style.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
